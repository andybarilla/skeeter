---
id: US-015
title: Add task dependencies (blocking relationships)
status: done
priority: critical
assignee: opencode
tags: [core, workflow]
created: "2026-02-20"
updated: "2026-02-20"
---

## Acceptance Criteria

- [x] Add `depends_on` field to Task struct (FlowSlice type)
- [x] `skeeter show US-001` displays blocked-by/blocking information
- [x] `skeeter list --blocked` shows only tasks with unmet dependencies
- [x] `skeeter next` skips tasks with incomplete dependencies
- [x] `skeeter create US-002 --depends US-001` flag support
- [ ] Desktop app shows dependency indicator on cards (deferred - requires frontend work)
- [x] SKEETER.md documents dependency workflow for agents

## Context

This enables proper task sequencing. A task cannot be started until all dependencies are done.

## Implementation Notes

- Added `DependsOn FlowSlice` field to Task struct
- Created `internal/store/dependencies.go` with:
  - `IsBlocked()` - checks if task has unmet dependencies
  - `GetDependencyStatus()` - returns full dependency status
  - `DetectCircularDependency()` - prevents circular deps on create
- Updated `skeeter show` to display blocked-by/blocking info
- Updated `skeeter list --blocked` to filter blocked tasks
- Updated `skeeter next` to skip blocked tasks
- Added `--depends/-d` flag to `skeeter create`
- Updated SKEETER.md with depends_on field documentation
