---
id: US-016
title: Add due date field to tasks
status: done
priority: high
assignee: opencode
tags: [core, ux]
created: "2026-02-20"
updated: "2026-02-20"
---

## Acceptance Criteria

- [x] Add `due` field (optional date string) to Task struct
- [x] `skeeter create` accepts `--due YYYY-MM-DD` flag
- [x] `skeeter list --overdue` shows past-due tasks
- [x] `skeeter list --due-this-week` shows tasks due in 7 days
- [ ] Desktop app highlights overdue tasks in red (deferred - requires frontend work)
- [ ] Desktop app shows due date badge on cards (deferred - requires frontend work)

## Context

Essential for project planning and time-sensitive work.

## Implementation Notes

- Added `Due string` field to Task struct
- Added `--due` flag to `skeeter create` with YYYY-MM-DD format validation
- Added `--overdue` flag to `skeeter list` to filter tasks past their due date
- Added `--due-this-week` flag to `skeeter list` to filter tasks due in next 7 days
- Updated `skeeter show` to display due date
- Updated `skeeter list` output to include DUE column
- Updated SKEETER.md with due field documentation
