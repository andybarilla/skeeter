---
id: US-017
title: Add JSON and YAML output formats to CLI
status: done
priority: high
assignee: opencode
tags: [cli, scripting]
created: "2026-02-20"
updated: "2026-02-21"
---

## Acceptance Criteria

- [x] `skeeter list --json` outputs JSON array of tasks
- [x] `skeeter list --yaml` outputs YAML list
- [x] `skeeter show US-001 --json` outputs single task as JSON
- [x] `skeeter next --json` outputs task as JSON (or null object if none)
- [x] JSON output includes all fields including body
- [x] Add global `--output` flag accepting table/json/yaml

## Context

Enables scripting, CI integration, and piping to other tools like jq.

## Implementation Notes

- Added global `-o/--output` flag to root.go accepting table/json/yaml
- Created `cmd/skeeter/output.go` with output helper functions
- Updated list.go, show.go, next.go to check output format and route accordingly
- Uses standard encoding/json and gopkg.in/yaml.v3 for marshaling
- When no tasks available, next outputs `null` in json/yaml modes
